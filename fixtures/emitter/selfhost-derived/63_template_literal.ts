function greet(name: string, n: number): string {
  return `hello, ${name}! count=${n}`;
}

function multiline(a: string, b: string): string {
  return `line1: ${a}
line2: ${b}
done`;
}

const empty: string = ``;
const plain: string = `no substitution here`;
